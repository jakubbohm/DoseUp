---
name: doseup-project-state
description: Founding interview 2026-07-13; c001 complete & archived 2026-07-16; PR #1 open + CI green — merge is Jakub's call; PM rollout signed off 2026-07-18 — repo-half PR open; GitHub execution pass DONE 2026-07-18 (tracker live #21–#69, old #6–#19 removed; doc patches in follow-up docs PR; one board-workflow UI check left); Claude Design workflow + Tailwind v4 decided 2026-07-18 (#81, PR #83 merged), amended 2026-07-19 (single workspace project, generation-time DS read — in-app conversation); configuration conventions decided 2026-07-19 (#40, PR #90 — appsettings.json IS the inventory, App Configuration deferred, issues #87/#89); self-service onboarding decided 2026-07-19 (#91, docs edits uncommitted — sign-up open, in-app complete-signup creates the account, admission gating = OQ-5); snake_case DB naming decided 2026-07-19 (PR #94; spike #93 GO via #96); c002-add-membership-domain implemented 2026-07-19 on `feat/c002-add-membership-domain` (Membership domain + schema, ApiResult/DomainResult split, placeholder retired); c002 archived 2026-07-20 pre-merge, PR #103 squash-merged 2026-07-20; #73 messaging idempotency resolved 2026-07-20 — idempotent-by-design consumers, inline listeners, no durable inbox (F-90) — rides PR #104; cleanup-branch skill created 2026-07-20 (PR #105, merge = Jakub's call)
metadata: 
  node_type: memory
  type: project
  originSessionId: 66159c46-e4d3-45a8-86cc-321fe9041b01
  modified: 2026-07-20T18:04:36.000Z
---

The founding product + engineering interview finished 2026-07-13. Everything it decided is recorded in the repo — treat these as authoritative, not memory: `docs/product/` (vision, requirements, roadmap), `docs/adr/0001-0004`, `docs/conventions/`, `CLAUDE.md` (working rules incl. [[user-gates-stage-progression]]), `openspec/config.yaml` (context + artifact rules).

The domain-layer decision resolved 2026-07-14; flagship `docs/conventions/domain-rules.md`. It *refined the earlier dispatch recording*: thin endpoints + framework-free feature handlers replaced the "endpoint = handler" shorthand — when an old summary conflicts with ADR-0002/conventions, the newest decision state wins. Jakub floated turning domain-rules.md into a skill later.

The testing-organisation decision resolved 2026-07-15; flagship `docs/conventions/testing.md` (ADR-0003 = stack, testing.md = organisation). Headlines: slice tests over HTTP = the handler test (no repositories ⇒ EF InMemory/SQLite/DbContext-mocks banned); isolation by construction (each test its own account, no cleanup); real JWT pipeline + test trust anchor; ADR-0002 gained dependency rule 7 (slice independence); Shouldly re-confirmed against TUnit.Assertions; zero-retry flake policy. It *refined the contract-pipeline rule*: contract modifications update every TS consumer in the same change (CI only compares, never commits); the TS facade/wrapper question stays an open design decision (Design decisions issues). testing.md §9 is the M0 verification checklist the shared-kernel change must walk.

Agreed sequencing (roadmap intro note): next is **one OpenSpec change for SharedKernel + test infrastructure** (implements the domain-rules primitives + testing.md layout/arch-test catalog, ships rules vacuously green), then **the first domain module as validation slice** (carries the Stryker×TUnit spike). Harvest factory artifacts (possible skills from domain-rules.md / testing.md) only after that slice proves the primitives.

**c001-add-shared-kernel** drafted 2026-07-15 on branch `feat/c001-add-shared-kernel` (proposal + 3 delta specs + design + tasks, all validating). Jakub's verdicts baked in: minimal ci.yml now; .editorconfig (copied from his Tastytrade repo) + CSharpier + strict-analysis props now, analyzer packs M0; empty DbContext + initial migration + MigrationService runner (Aspire's documented pattern); bearer + secure-by-default + identity-echo endpoint (no Entra); FV bridge + SmartEnum converter deferred to first consumer; **change-id rule amended to `cNNN-` ids** (openspec/config.yaml; OpenSpec CLI rejects digit-leading names — his verdict) — also fixed a config.yaml YAML bug (a `: ` inside a tasks rule made the CLI silently ignore ALL tasks rules). Ecosystem facts verified live in design.md D2/D3/D10 (dotnet.config→global.json MTP switch; TUnit.Engine-only to avoid TUnit.Assertions; union grammar = mandatory case-type list, no polyfill since preview 5; FastEndpoints has no net11 TFM — c001's first build IS the spike). Version strategy per Jakub's verdict: **greenfield starts on the freshest preview** — SDK/runtime/ASP.NET = preview 6 (`11.0.100-preview.6.26359.118`), EF+Npgsql held at preview.5 (Npgsql has no preview.6 yet and exact-pins EF preview.5; bump when it ships). **Applied 2026-07-15** (opsx:apply): 23/25 tasks done, 73/73 tests green (45 unit · 16 arch · 12 integration incl. the live Aspire harness), CSharpier + openspec validate clean, docs corrected (testing.md §4/§9, ADR-0001 union caveats, roadmap M0, domain-rules §2 grammar). Implementation-discovered facts live in design.md (D3 spike results incl. nested-case qualified list + no generated equality; D11 ArchUnitNET TUnit-adapter dropped — hard-deps TUnit.Assertions 0.52.51; D6 pg_stat_activity probe proof; exact SDK build 26359.118 not 26328.106). **All 25/25 tasks done**: committed on `feat/c001-add-shared-kernel`, PR #1 open, ci.yml green on the PR (1m34s — needed a `.gitattributes` CRLF rule for `*.cs` so Linux checkouts match the CSharpier-enforced editorconfig ending). Jakub then reviewed every file topic-by-topic (next paragraph); /opsx:archive ran 2026-07-16 after the review wrapped.

**Formatting flip (2026-07-15, after PR #1 opened, task 7.5):** Jakub saw the output and rejected CSharpier's Allman braces — his `.editorconfig` (end-of-line braces, `csharp_new_line_before_open_brace = none`) is the **single C# formatting authority** now. CSharpier removed (props/CI flag/`.vscode`); IDE0055 re-armed → **the build is the format gate** (probe-verified to error on an Allman brace); `dotnet format whitespace DoseUp.slnx` is the fixer; width = soft 200-char guideline, deliberately unenforced ("less than half my screen"). F-34 deposited as a reversal. Lesson (now in F-34): during c001, the CSharpier-vs-editorconfig brace conflict was resolved by muting IDE0055 as a design-doc footnote — never silence the owner's rules to fit a tool without surfacing the decision. When writing C# here: braces on end of line, 2-space indent; a plain build catches any slip.

Same day, follow-up (task 7.6): **one-line blocks carry no braces, statement on the next line indented** — Roslynator.Analyzers 4.15.0 adopted **scoped to RCS1002 only** (`category-Roslynator = none` kill-switch in .editorconfig; build error via TWAE). Next-line placement is convention only (`allow_embedded_statements_on_same_line` stays true). **the Roslynator-pack review** recorded as an open design decision (Design decisions issues) — Jakub may widen the pack later, he kicks it off. F-74 deposited (direction-of-rule survey: only Roslynator has the remove direction; Sonar/StyleCop enforce the opposite).

**c001 review progress (Jakub, topic-by-topic on PR #1; commits land only on his "commit"):** 7.5 formatting flip · 7.6 RCS1002 · 7.7 legacy-wrap sweep · 7.8 member layout + the repo's **first `.claude/rules/` file** (`csharp-member-layout.md`) · topic 6 = VS Code null-switch squiggle explained as IDE-Roslyn version skew (explain-only, no code change — the exhaustiveness build error is a designed safety property, never add a discard arm to the Result mapper switch) · `formatOnType` he committed himself · **the Result.cs revisit** (his dislike, deferred to first real slice) and **per-module service registration** (ServiceCollectionExtensions per module) recorded as open design decisions (Design decisions issues) — he kicks those off. **Task 7.9 (2026-07-15/16, ultracode workflow — 9 agents + adversarial verify):** persistence re-decided as **module property** (module = DDD bounded context; module-owned DbContext/schema/migrations; UoW = the module's context, conscious DDD divergence; catalog rules 17–19 + revised 4/10 + tripwire; Wolverine per-module-store = M0 spike go/no-go; c001's DoseUpDbContext relabeled bootstrap placeholder, removed in M0 by the Membership change per D13 2026-07-17 re-scope — was M1); modules renamed to **capability names** (gerund where natural, never forced): **Membership** (accounts + profiles — the cared-for entity is **`Profile`**, not Subject/Patient) and **Scheduling**; F-75/F-76 deposited. 77/77 tests. All review work committed & pushed (through `4120457`), PR #1 CI green (1m26s), PR body rewritten to match. **/opsx:archive (2026-07-16):** openspec-sync-specs created the repo's first main specs — `openspec/specs/{shared-kernel-primitives (11 req), error-contract (5), api-shell (3)}` (`validate --specs --strict` 3/3) — and the change dir moved to `openspec/changes/archive/2026-07-16-c001-add-shared-kernel/`. Sync + archive are **uncommitted** on the branch (Jakub's sequencing: the archive commit rides the open PR, squash-merge lands it all). Next is his: commit the archive set, merge PR #1.

`docs/software-factory.md` is special to Jakub: a reusable decision catalog seeding a future project-setup ("software factory") repo — the top-priority deliverable of this phase. Keep depositing (F-1..F-76 so far) when foundational decisions are made or reversed — reversals are explicitly wanted as training data.

Process rhythm that works: per-topic interview (propose → Jakub verdicts → land docs → next topic; flag when a "verdict" has no real fork — he dislikes empty questions); each resolved design decision lands its outcome in the authoritative docs + factory deposits + its own commit, then its `decision` issue closes with a short pointer comment (see [[github-project-management]]). `.editorconfig` and ci.yml exist since c001; `.claude/rules/` exists since 2026-07-15 (first file: csharp-member-layout.md) — the broader docs/skills/memory re-organisation stays an open design decision (Design decisions issues), Jakub kicks it off.

**PM rollout (signed off by Jakub 2026-07-18):** the plan's three open questions resolved by accepting the recorded leans — `enhancement` stays on the pipeline top-levels, `ci-ordering` stays milestone-less, `opsx` stays on the CI/CD tree. This PR (`chore/project-management-rollout`) carries the **repo half**: the human-first GitHub tracker process (`docs/conventions/project-management.md`, F-77..F-80 deposited, memory [[github-project-management]]); PRE-x naming swept repo-wide to full-slug artifact links; `docs/product/pre-implementation-checklist.md` deleted (its 20 items migrate to `decision` issues under the evergreen "Design decisions" parent); openspec/config.yaml Tracks/Refs + Closes-line + verify gate; roadmap re-scoped (Membership module + get-account-detail slice into M0, bootstrap-placeholder retirement M1→M0). After it merges, the **GitHub execution pass** runs on Jakub's explicit go: labels 18→12, milestone descriptions, board field deletions, delete old issues #6–#19, create + wire 49 issues, patch 9 doc spots with real issue numbers. The signed-off plan with the full execution checklist lives at `C:/Users/jakub/AppData/Local/Temp/claude/D--source-jakubbohm-DoseUp/fb10921b-8a62-4ce6-bfaa-65c4e3ea0b70/scratchpad/m0-plan.md` (absolute path — session scratchpad, not part of the repo).

**GitHub execution pass completed 2026-07-18:** the tracker is **live** — 49 issues created (**#21–#69**), old #6–#19 permanently removed. The evergreen **"Design decisions" parent = #21**, its 20 decision sub-issues = #22–#41 (11 resolved & closed as Done, 9 open), the 28 M0 work issues = #42–#69. Labels rebuilt to the 12-set, four board fields deleted, milestone descriptions rewritten, all 49 on project 6 with Status set (closed decisions → Done, rest → Todo). **Doc patches shipped in a follow-up `docs:` PR** (the same PR carrying this memory update) — the marker phrase "tracked in the Design decisions issues" was swept from 10 spots (9 plan doc/code spots + `.editorconfig`) to concrete issue links (`d05`→#26, `d16`→#37, `d18`→#39). **One manual UI step still outstanding for Jakub (~1 min):** verify project 6 Workflows — "Item closed" sets Status=Done and "Auto-add to project" targets this repo (GraphQL can't configure workflows; API-verified all seven are enabled).

**Claude Design workflow decided 2026-07-18 (#81, blocker of #58 → docs PR):** flagship `docs/conventions/design.md` — the repo owns tokens/components; `/design-sync` pushes up (push-only; "two-way" is third-party spin), design work returns only as "Hand off to Claude Code" bundles, the tool never writes to the repo (claude.ai GitHub integration is read-only). **Tailwind CSS v4 = Jakub's styling verdict** (ADR-0001 amended; #26 React data & state stack stays open — the 2026-07-18 fork re-examined Tailwind and confirmed it, adding Radix Primitives + the latest-iOS floor, merged 2026-07-19). Design-tool state: project **"DoseUp"** (id `6ada98c2-bda4-4278-9995-50cee0d099db`) created from Claude Code 2026-07-18; the empty onboarding project "Design System" is never used (Jakub may delete it in the app). #58 re-scoped to the bootstrap chain (design.md § 5). Capability facts verified 2026-07-18 by a 5-verifier workflow + 21-agent review (ultracode) — research preview, re-verify on next pickup.

**Design workflow amended 2026-07-19** from Jakub's in-app workflow conversation with Claude Design (transcript archived on #81): the design system is read at **generation time**, not copied at project creation → **one persistent design-workspace project** with feature folders (fresh-per-iteration superseded; Jakub prefers this); designs are snapshots → redesigns start from the synced recreations of shipped screens; handoffs are feature-scoped, stack-agnostic spec bundles whose README defers to the repo's conventions (full export only for the greenfield first-screens drop); translation maps onto existing tokens — new ones are flagged decisions (rule mirrored in CLAUDE.md + openspec/config.yaml). These claims are the product describing itself — design.md § 6 keeps them on the confirm-on-first-use list. Jakub's workspace project already holds root files `DoseUp.dc.html`, `DoseApp.dc.html`, `DoseKit.dc.html` (seen in that conversation).

**Configuration conventions decided 2026-07-19 (#40) — PR #90, merge is Jakub's call.** Flagship `docs/conventions/configuration.md`. The one rule: **the app reads only `appsettings.json` + env vars; everything platform-specific lives in Bicep** (no Azure SDK in the config path — the Key Vault *configuration provider* is rejected as entanglement; ACA resolves KV secrets into plain env vars). `appsettings.json` **is** the settings inventory (Jakub's pushback killed a proposed markdown table — the file the app reads can't drift as far): every setting declared, secrets as the literal `"<secret>"` = greppable marker **and** startup tripwire, provenance in `//` comments. `IOptions<T>` + `ValidateOnStart`; `IConfiguration` banned outside Platform composition (new arch-catalog rule 20). **No `appsettings.Production.json`** (base = production truth). **Azure App Configuration deferred** — reverses the founding pick: flags ride `IConfiguration`, prod overrides are `.bicepparam` env vars (flip without rebuild), plus a *bounded break-glass* `az containerapp update` carve-out for incidents. GitHub Actions *variables* rejected except pre-Bicep pipeline targeting. Verified in-session: JSON config provider skips `//` comments; the harness inherits the **AppHost's** user secrets but **only while the environment is Development** (silent skip otherwise — testing.md §9 item 9); ACA `keyVaultUrl`+`identity`→`secretRef`, version-less URIs auto-refresh and restart revisions. Touched: configuration.md (new), conventions/README.md, testing.md (rule 20 + §9.9), ADR-0001 (flag row), ADR-0004 (env-vars-from-Bicep, break-glass, Actions-variables rejection, new task rule), openspec/config.yaml, software-factory.md (new **§ J** F-82..F-85, F-86 test secrets, F-33 reversal, scope-rot header warning), `.claude/rules/configuration.md` (new), CLAUDE.md. Issues created: **#87** (frontend config — no milestone) and **#89** (typed+validated options, M0). **#87 revised 2026-07-19 after Jakub's pushback:** the web app is served from a *free static host on its own origin* (SWA linked-backend proxy, which would give same-origin, needs the Standard plan), so frontend config is a **`config.json` the release pipeline writes beside the bundle**, fetched from the app's own origin. Fetching it from the API is **rejected** — that call needs the address it is fetching. A config *endpoint* is reserved for reload-required values (flags) only. Bundle = promoted artifact, `config.json` = deployment metadata, so build-once holds. Consequences: CORS allowlist + preflight, `config.json` excluded from SW precache and long caching, local value is `/api` via the Vite dev proxy. **Hosting is now recorded in ADR-0001** — the old `Hosting` row split into `Hosting (API)` = Container Apps and `Hosting (web app)` = Static Web Apps Free; ADR-0004's build-once clause gained the web bundle. Note for future sessions: SWA Free *does* include 2 custom domains — what forces cross-origin is that the linked-backend proxy needs Standard, not the domain. #40 closes via the PR body. Jakub's standing lesson from this round: **DoseUp answers are not automatically generic** — software-factory entries must name the constraint that would reverse them (his example: harness-needs-no-secrets is false when the external service is the thing under test).

**Database naming decided 2026-07-19 — snake_case via EFCore.NamingConventions (PR #94, squash-merged 2026-07-19).** Jakub's pick after the Postgres-vs-MS-SQL casing question: every DB identifier snake_case (`UseSnakeCaseNamingConvention()` per module context), C# stays PascalCase; driver = Postgres case folding (quoted-PascalCase taxes hand-run ops SQL). Recorded in conventions/README.md § Persistence + software-factory **F-88** (marked Postgres-specific — on SQL Server the ORM default is correct). Package's latest (10.0.1) targets EF 10 while services ride EF 11 previews → **spike #93** (M0, `spike`+`db`, native blocked-by on #42) proves it before the first migration; named fallback = hand-rolled `IModelFinalizingConvention` (~20 lines), decision stands either way. Done via worktree, branch `docs/db-snake-case-naming` (worktree removed after push).

**c002-add-membership-domain implemented 2026-07-19 (branch `feat/c002-add-membership-domain`; tasks 1–6 done, GitHub pass + PR + verify + archive pending).** Artifacts 4/4 (Tracks: #53 #55 #97 #98; #38 commented-not-closed). Session decisions, all landed docs-first: `AccountStatus {Active=1, Disabled=2}` — **admin is neither a state nor a flag** (permission model = future change; admin-trace swept from ADR-0002 ring 1, conventions § Authorization, config.yaml, CLAUDE.md; caller class `revoked`→`disabled`) · rule checks named **`CheckCanXxx`** (domain-rules.md examples renamed) · **`Result`→`ApiResult`** (#98) + new **`DomainResult`** union — the #38 *domain* half; `DomainResult<T>` shipped consumer-less by Jakub's explicit growth-rule exception; `SignUp` stays rule-free (set rule = handler + unique constraint); #38 stays open for the handler half, judged against #54's call sites · **arch rule 21** (Domain never references ApiResult; deliberate-violation-proven — evidence in session scratchpad `rule21-proof.md`, belongs in the PR body) · spike **#93 GO** baked in: three authoring rules + the NamingConventions **expiry at EF 11 stable** (`[10.0.1, 11.0.0)` → NU1608) recorded in conventions § Persistence, owner = preview-bump activity · `Ardalis.SmartEnum(.EFCore)` 8.2.0 — EFCore companion dormant-but-GO (verified 2026-07-19), registration explicit per-type, 4-line hand-roll fallback recorded · CA1508 union false positive: exemption extended to `Modules/**` (.editorconfig, c001 precedent) · Stryker **deferred out** (testing.md §7 + roadmap re-slotted, M2 recurrence at the latest). 97/97 tests incl. Aspire harness — membership migration proven on real Postgres. Archive-time duty: rename `Result` in the two main-spec **Purpose** paragraphs during sync (tasks 8.1).

**c002 review response + #99 folded in 2026-07-20 (same branch, tasks 9.1–9.7; PR body now closes #53 #55 #97 #98 #99).** Jakub's one-by-one review verdicts: inference-first EF config (`IsRequired()` removal — one call was invisibly load-bearing: setterless `CreatedAt`'s `Property()` call IS its mapping; convention + F-89 in software-factory) · `default(DomainResult)` crash stays designed (c001 "never add a discard arm" reaffirmed; pin test added) · CA1508 exemption stays module-wide, removal tracked in **#101 under new #100 ".NET 11 GA" umbrella**, NamingConventions expiry now **#102** (same umbrella) · guards: null-status on `CheckCanXxx`, shared `RuleViolationCarrier` (guard + snapshot) for all violation cases, `ApiResult.Validation` deep-guarded (per-field arrays) · **#99 implemented within c002**: `DomainResult` + whole `Rules` vocabulary (incl. async `RuleSet` — nuance recorded in D8) merged into `SharedKernel/Domain`; conversions inverted to `ApiResult.From(...)` — domain side mentions the edge nowhere, comments included (Jakub caught comment leaks); rule 21 = direction rule (module Domain + SharedKernel.Domain ↛ SharedKernel.Results), `typeof`-anchored, deliberate-violation-re-proven · typed-id registration module-scoped (`(assembly, namespacePrefix)`, null = deliberate hatch) — Jakub overrode my keep-the-sweep recommendation: boundary knowledge counts even when model-inert. Two 16-agent adversarial verification workflows ran (review-response diff): 7 distinct confirmed findings, all fixed. All 10 original review findings resolved (finding 10 accepted 2026-07-20: DisplayName/Email get-only + explicit `Property()` mappings per F-89). 122/122 tests. **PR #103 opened + /opsx:archive run 2026-07-20 (Jakub's call, pre-merge — same pattern as c001; /opsx:verify skipped):** specs synced (`membership-accounts` main spec born — 7 req; ApiResult renames + DomainResult requirements into shared-kernel-primitives; mapper rename + Purpose fix into error-contract; `validate --specs --strict` 4/4), task 8.1 done → 22/22, change dir → `openspec/changes/archive/2026-07-20-c002-add-membership-domain/`. Sync + archive rode PR #103, **squash-merged to main 2026-07-20** (`656a421`) — c002 fully landed.

**Messaging idempotency resolved 2026-07-20 (#73 — docs PR #104, merge is Jakub's call).** Jakub challenged the "we need the inbox anyway" framing and was right on every count (verified against current Wolverine/Azure docs): the durable inbox = store-and-forward + a 5-min message-id dedup window (`Durability.KeepAfterMessageHandling`) — it can't carry correctness, duplicates ASB Basic's native durability/peek-lock-redelivery/DLQ, and its immediate ack hides pending envelopes from the KEDA queue-depth wake chain (buffered mode even loses messages — acks before processing). Decision: consumers idempotent **by design** (business-key re-query + DB unique backstop, the messaging-poc spike pattern), listeners explicitly `ProcessInline()` + native ASB dead-lettering, durable inbox **not enrolled** (per-module incoming-envelope tables stay empty), any future opt-in owes a wake story first; outbox side untouched. ASB Basic has no scheduled messages (Standard+) — irrelevant: reminders ride F-47's storage-queue visibility alarms. Landed: ADR-0002 § Events receive-side bullet + line-120 adoption-artifact addendum, ADR-0001 async row + topology bullet, conventions/README § Events, domain-rules § 8, software-factory **F-90**.

**cleanup-branch skill created 2026-07-20 (PR #105, merge is Jakub's call).** `.claude/skills/cleanup-branch` — strictly gated cleanup of the dangling local branch that squash-merge + delete-on-merge leaves behind; design and rationale live in the SKILL.md and PR #105's body, not here. Six refusal gates were exercised live pre-PR; the happy path gets its first real exercise cleaning up this PR's own branch (`chore/cleanup-branch-skill`) after merge — a good moment to confirm it end-to-end.

**Self-service onboarding decided 2026-07-19 (#91 under #21; docs edits UNCOMMITTED in the working tree — commit/PR is Jakub's call).** Jakub's exact scope ruling: users sign themselves up (he only hands out the URL); an authenticated caller **without an account row** is forced into an in-app **complete-signup** that ends by creating the account; **everything beyond that (account states, approval/blocked, how the circle stays private) is deliberately NOT decided** — registered as **OQ-5** in the vision, owed before M3's account-admin change; he explicitly rejected deciding it in-session. Reversals this implies (he confirmed by directing the change): the "require user assignment" IdP mirror in ADR-0001/F-51 is **reversed**; #75's "disabled self-service sign-up" lean is gone (API→Graph credential = N/A; user flow created with sign-up enabled). Docs touched: ADR-0001, ADR-0002 § Authorization (new "Identity ≠ membership, onboarding is self-service" paragraph; Ring-1 lifecycle wording; `stranger` caller class), FR-1/FR-2, vision (circle bullet + OQ-5), roadmap M3 (`add-account-admin`, complete-signup polish), testing.md (§3c stranger fixture + §4 matrix column), configuration.md (user-flow posture), software-factory (F-51 revision + new F-87). Issues #75/#47 bodies patched, #91 created + sub-issue-linked to #21. Facts verified against MS Learn 2026-07-19: External ID has NO customer-invitation feature (admin-center invites = admin accounts only); `isSignUpAllowed=false` is Graph-beta-only AND blocks federated auto-creation. Still-open observation (flagged twice, not yet an issue): how Jakub's own first admin account gets seeded for M0's done-when — complete-signup may cover creation, but the admin flag needs a bootstrap answer.
